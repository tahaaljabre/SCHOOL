import { signInExternal } from "../../../../db/external-auth";
export async function POST(request:Request){try{const p=await request.json() as Record<string,unknown>,identifier=String(p.identifier||""),password=String(p.password||""),type=String(p.type||"parent"),session=await signInExternal(identifier,password,type);return Response.json({message:"تم تسجيل الدخول"},{headers:{"set-cookie":`school_session=${encodeURIComponent(session.access_token)}; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=${session.expires_in||3600}`}});}catch(e){return Response.json({error:e instanceof Error?e.message:"تعذر تسجيل الدخول"},{status:401});}}
