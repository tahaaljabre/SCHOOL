import { requireSchoolAdmin } from "../../../db/authorization";
export async function GET(){try{const auth=await requireSchoolAdmin();if(auth.error)return auth.error;const [entries,classes,teachers,subjects,settings,shifts]=await Promise.all([auth.db.prepare(`SELECT e.*,c.name classroom_name,t.full_name teacher_name,s.name subject_name,c.shift_id FROM schedule_entries e JOIN classrooms c ON c.id=e.classroom_id JOIN teachers t ON t.id=e.teacher_id JOIN subjects s ON s.id=e.subject_id ORDER BY e.day_of_week,e.period_number`).all(),auth.db.prepare("SELECT id,name,shift_id FROM classrooms WHERE archived_at IS NULL").all(),auth.db.prepare("SELECT id,full_name name FROM teachers WHERE archived_at IS NULL").all(),auth.db.prepare("SELECT id,name FROM subjects WHERE archived_at IS NULL ORDER BY name").all(),auth.db.prepare("SELECT work_days FROM school_settings WHERE id=1").first<{work_days:string}>(),auth.db.prepare("SELECT id,name,starts_at,ends_at,gender_scope FROM shifts WHERE status='ACTIVE' ORDER BY id").all()]);return Response.json({entries:entries.results,classes:classes.results,teachers:teachers.results,subjects:subjects.results,workDays:JSON.parse(settings?.work_days||"[6,0,1,2,3]"),shifts:shifts.results});}catch{return Response.json({error:"تعذر تحميل الجدول"},{status:500});}}
export async function POST(request:Request){try{const auth=await requireSchoolAdmin();if(auth.error)return auth.error;const p=await request.json() as Record<string,unknown>,classroomId=Number(p.classroomId),teacherId=Number(p.teacherId),subjectId=Number(p.subjectId),day=Number(p.dayOfWeek),period=Number(p.periodNumber);const settings=await auth.db.prepare("SELECT work_days FROM school_settings WHERE id=1").first<{work_days:string}>(),workDays=JSON.parse(settings?.work_days||"[]") as number[];if(!classroomId||!teacherId||!subjectId||!workDays.includes(day)||period<1||period>8)return Response.json({error:"اختر يوم عمل صحيحًا وأكمل بيانات الحصة"},{status:400});const classroom=await auth.db.prepare("SELECT shift_id FROM classrooms WHERE id=? AND archived_at IS NULL").bind(classroomId).first<{shift_id:number}>();if(!classroom?.shift_id)return Response.json({error:"يجب تعيين الفصل إلى فترة أولًا"},{status:400});const assigned=await auth.db.prepare("SELECT 1 ok FROM teacher_shifts WHERE teacher_id=? AND shift_id=?").bind(teacherId,classroom.shift_id).first();if(!assigned)return Response.json({error:"المعلم غير معين في فترة هذا الفصل"},{status:403});const result=await auth.db.prepare("INSERT INTO schedule_entries(classroom_id,teacher_id,subject_id,day_of_week,period_number,starts_at,ends_at) VALUES(?,?,?,?,?,?,?)").bind(classroomId,teacherId,subjectId,day,period,String(p.startsAt||"07:00"),String(p.endsAt||"07:45")).run();return Response.json({message:"تمت إضافة الحصة",id:result.meta.last_row_id},{status:201});}catch(e){return Response.json({error:String(e).includes("UNIQUE")?"يوجد تعارض للمعلم أو الفصل في هذه الحصة":"تعذر إضافة الحصة"},{status:400});}}
export async function DELETE(request:Request){try{const auth=await requireSchoolAdmin();if(auth.error)return auth.error;const id=Number(new URL(request.url).searchParams.get("id"));await auth.db.prepare("DELETE FROM schedule_entries WHERE id=?").bind(id).run();return Response.json({message:"تم حذف الحصة"});}catch{return Response.json({error:"تعذر حذف الحصة"},{status:400});}}
