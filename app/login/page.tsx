"use client";
import {useState} from "react";
export default function LoginPage(){const [type,setType]=useState("student"),[identifier,setIdentifier]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type,identifier,password})}),d=await r.json();if(!r.ok)throw new Error(d.error);location.href="/";}catch(e){setError(e instanceof Error?e.message:"تعذر تسجيل الدخول")}finally{setLoading(false)}};const username=type!=="parent";return <main className="school-login" dir="rtl"><section><div className="login-brand"><span>ص</span><div><strong>مدرسة الصومعه</strong><small>يافع — المفلحي — اليمن</small></div></div><h1>تسجيل الدخول</h1><p>اختر نوع الحساب ثم أدخل بياناتك المدرسية.</p><div className="login-tabs three"><button className={type==="student"?"active":""} onClick={()=>setType("student")}>طالب</button><button className={type==="parent"?"active":""} onClick={()=>setType("parent")}>ولي أمر</button><button className={type==="staff"?"active":""} onClick={()=>setType("staff")}>معلم / إدارة</button></div><form onSubmit={submit}>{error&&<div className="form-error">{error}</div>}<label>{username?"اسم المستخدم":"البريد الإلكتروني"}<input autoComplete="username" value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder={username?(type==="staff"?"مثال: T101":"مثال: ST1024"):"name@example.com"}/></label><label>كلمة المرور<input type="password" autoComplete="current-password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary" disabled={loading}>{loading?"جارٍ الدخول...":"دخول إلى البوابة"}</button></form><small className="login-help">إذا نسيت بيانات الدخول، تواصل مع إدارة المدرسة لإصدار كلمة مرور مؤقتة.</small></section></main>}
